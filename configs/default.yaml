# Hydra-style run config. One file per experiment; the ablation runner sweeps these.
backbone: dav2-vits          # dav2-vits | dav2-vitb | dav2-vitl | dpt-large | synthetic
chip: 1024
overlap: 0.25

dem_source: null             # copernicus | srtm | path to a GeoTIFF
gcp_file: null
reference: null              # ground-truth DSM, for validation only
use_icesat: false
test_time_refinement: false

# AGMC (Phase 2)
n_bootstrap: 24
lattice_stride: 32
lam_a: 1.0
lam_b: 1.0
huber_delta: 2.0
irls_iters: 3
